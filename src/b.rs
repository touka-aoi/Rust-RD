use proconio::input;

fn main(){
    input!{
        sx: f32,
        sy: f32,
        gx: f32,
        gy; f32,
    }
    println!("{} {} {} {}", sx, sy, gx, gy);
    println!("{}",sx + (((gx - sx)/(sy + cy)) * sy);
}
